use std ( add, A, io )

mod std {
    fn add(v: int, v2: int) : int { v + v2 }

    struct A { i: int }


    mod io {
        struct IO {i: A}
    }
}

@startup
system fn main(): bool { 
    69 as bool 
}
