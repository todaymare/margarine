use std ( A, io/IO, io )

mod std {
    fn add(v: int, v2: int) : int { v + v2 }

    struct A { i: int }


    mod io {
        struct IO {i: A}
    }
}


fn main(io: io::IO): int {
    add(69, 420)
}
