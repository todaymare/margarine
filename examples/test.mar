extern "https://github.com/todaymare/margarine-std" as std;
use std::hashmap::*;

fn main() {
    var hasher = Hasher::new();
    hasher.write_int(69);
    hasher.write_int(420);
    std::println(hasher.finalize());


    var hm = HashMap {
        tables: [],
        entry_count: 0,
    };

    hm.insert("boobs", "fart");
    hm.insert("boobs", "whatever");
    std::println(hm.get("boobs")!);
}


