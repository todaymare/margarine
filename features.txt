// This is a line comment
// We do not have block comments

// A component can be attached to
// entities and queried by systems
component Position {
	x: int,
	y: int,
}

enum Option {
	some: any,
	none: int,
}

fn hi(&hey: int): int {
	match Option::none(69) {
		some => hey,
		none => 5,
	}
}
