impl Range {
    fn __next__(&self): Option<i64> {
        self.min += 1

        if self.min > self.max { none() }
        else { some(self.min - 1) }
    }
}


impl<T> Ptr<T> {

}


extern {
    fn print(str: str)
    fn print_i64(str: i64)
}

